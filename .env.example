# Copy to `.env` and fill in only when going live.
#
# The harness ships with USE_STUB = True (see backend/config.py): every Anthropic
# call returns a canned, correctly-shaped payload, so the whole system runs end to
# end with NO API key. You only need a key once the researcher flips USE_STUB=False.

ANTHROPIC_API_KEY=
