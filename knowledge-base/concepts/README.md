# concepts/

The *why* of the study — the ideas the code implements. Start here before the area folders.

- [[The Experiment C2 vs C3]] — the core comparison (fixed clarifier vs generated interface) + C1 baseline.
- [[One-shot Two-call Interaction]] — why it's two model calls, not a chat; where execution sits.
- [[Ambiguity Classes]] — the four kinds of question ambiguity and which database hosts each.
- [[Overreliance Probe]] — two-stage confidence + under-specified items to surface confident-wrong answers.
- [[Stub vs Live]] — stub vs real model call, and the per-session page-0 switch.

See also: [[Design Overview]] (the one-page rationale) and the meta-README at the vault root.
