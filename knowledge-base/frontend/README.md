# frontend/

The React + Vite participant wizard (plain JSX). Each code note has a plain-English "How it works"
walkthrough and a link to the real source file.

- [[Frontend Overview]] — how the SPA is wired; runs locally with the backend.
- [[Wizard App]] — the screen-by-screen state machine (incl. page 0).
- [[API Client]] — the typed calls to the backend (via the `/api` proxy).
- [[C2 Static Interface]] — the fixed multiple-choice clarifier.
- [[C3 Dynamic Host]] — live-mounts the model's generated JSX in a sandbox.
- [[C3 Primitives]] — the whitelisted UI building blocks for C3.
- [[Copy]] — all participant/operator text in one JSON file.
- [[Screen Components]] — the individual wizard screens + shared pieces.
