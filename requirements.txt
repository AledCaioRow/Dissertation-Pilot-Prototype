# --- Web application (live participant session) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0
tenacity>=8.2

# Anthropic SDK — imported only in the stubbed call layer (backend/calls/_stub.py).
# Not actually called until USE_STUB=False.
anthropic>=0.40

# --- Post-hoc analysis notebook (analysis/analysis.ipynb) ---
jupyter>=1.0
pandas>=2.1
scipy>=1.11
statsmodels>=0.14
