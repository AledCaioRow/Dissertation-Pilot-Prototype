@echo off
REM One-step local launch (Windows) — double-click this file.
REM Runs the backend + frontend in STUB mode out of the box (no API key needed).
powershell -ExecutionPolicy Bypass -File "%~dp0start.ps1"
